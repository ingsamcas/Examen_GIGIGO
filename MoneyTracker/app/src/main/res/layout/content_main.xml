<?xml version="1.0" encoding="utf-8"?>
<com.sothree.slidinguppanel.SlidingUpPanelLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:sothree="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="bottom"
    android:layout_marginTop="?attr/actionBarSize"
    sothree:umanoPanelHeight="60dp"
    sothree:umanoShadowHeight="4dp"
    sothree:umanoParallaxOffset="100dp"
    sothree:umanoDragView="@+id/dragView"
    sothree:umanoOverlay="true"
    tools:context=".activities.MainActivity"
    tools:showIn="@layout/activity_main"
    android:background="@color/cardview_light_background"
    android:id="@+id/content_main_slidingup">

    <RelativeLayout
        android:paddingBottom="60dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/content_main_maincontainer" />
    <!-- SLIDING LAYOUT -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/content_main_slidecontainer"
        android:orientation="vertical"
        android:background="@color/cardview_light_background">

        <LinearLayout
            android:layout_alignParentTop="true"
            android:id="@+id/content_main_slidecontainer_showed"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:orientation="vertical" />
        <LinearLayout
            android:background="@color/cardview_light_background"
            android:layout_below="@+id/content_main_slidecontainer_showed"
            android:id="@+id/content_main_slidecontainer_hided"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </RelativeLayout>
</com.sothree.slidinguppanel.SlidingUpPanelLayout>
